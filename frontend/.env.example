# TraceX Frontend Environment Variables
# In production on Vercel, set this to your deployed Render backend URL (no trailing slash):
VITE_API_URL=https://tracex-backend-xgpq.onrender.com

# For local development (leave blank to use Vite's built-in proxy, or set explicitly):
# VITE_API_URL=http://localhost:8000
